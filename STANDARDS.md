# Sorcha Standards Compliance

Sorcha is cryptographic proof infrastructure for multi-party workflows. The standards below underpin that posture: BIP32/39/44 for hierarchical-deterministic wallet keying, ML-DSA (NIST FIPS 204) for post-quantum signatures on the internal path, ML-KEM (FIPS 203) for post-quantum key encapsulation, JSON Pointer–based selective disclosure inside SD-JWT VC envelopes, Merkle dockets for ledger integrity, and the OpenID4VC + HAIP profile for wallet interoperability at the boundary. This file is the single source of truth for those claims; every other surface that names a standard (`llms.txt`, OpenAPI `info.x-standards`, `docs/` frontmatter) cross-references a row here.

Honest gaps named explicitly: HAIP 1.0 mandates classical signatures at the wire boundary, so the post-quantum posture is internal only; BBS+ is not yet implemented; mTLS is not yet enforced on inter-service hops outside the gateway.

Status values:

- `full` — the standard is fully implemented for the component(s) listed.
- `partial` — the standard is partially implemented; the Notes column names the specific gap.
- `planned` — the standard is on the roadmap but not yet implemented; the Notes column names the spec or roadmap item that will deliver it.

| Standard | Version | Body | Spec URL | Components | Status | Notes |
|---|---|---|---|---|---|---|
| BIP32 | 2017 | Bitcoin Improvement Proposals | [BIP-0032](https://github.com/bitcoin/bips/blob/master/bip-0032.mediawiki) | `src/Core/Sorcha.Wallet.Core/Services/Implementation/KeyManagementService.cs` | full | NBitcoin-backed; HD path-style derivation across all wallets |
| BIP39 | 2013 | Bitcoin Improvement Proposals | [BIP-0039](https://github.com/bitcoin/bips/blob/master/bip-0039.mediawiki) | `src/Core/Sorcha.Wallet.Portable/Domain/ValueObjects/Mnemonic.cs` | full | English wordlist only |
| BIP44 | 2014 | Bitcoin Improvement Proposals | [BIP-0044](https://github.com/bitcoin/bips/blob/master/bip-0044.mediawiki) | `src/Common/Sorcha.Wallet.Contracts/Constants/SorchaDerivationPaths.cs` | full | Sorcha-specific purpose namespace per derivation slot |
| ML-DSA (FIPS 204) | 2024 | NIST | [FIPS 204](https://csrc.nist.gov/pubs/fips/204/final) | `src/Common/Sorcha.Cryptography/Core/PqcSignatureProvider.cs`, `src/Common/Sorcha.Cryptography/Core/CryptoModule.cs` | full | Internal signing path; HAIP wire boundary remains classical per HAIP 1.0 |
| ML-KEM (FIPS 203) | 2024 | NIST | [FIPS 203](https://csrc.nist.gov/pubs/fips/203/final) | `src/Common/Sorcha.Cryptography/Core/CryptoModule.cs` | full | Internal key-encapsulation path |
| OpenID4VCI | 1.0 | OpenID Foundation | [OpenID4VCI](https://openid.net/specs/openid-4-verifiable-credential-issuance-1_0.html) | `src/Services/Sorcha.Haip.Service` | partial | Pre-authorised issuer endpoint per spec 097; SD-JWT VC issued as `dc+sd-jwt` final media type (Feature 181 US1) |
| OpenID4VP | 1.0 | OpenID Foundation | [OpenID4VP](https://openid.net/specs/openid-4-verifiable-presentations-1_0.html) | `src/Services/Sorcha.Haip.Service`, `src/Common/Sorcha.Verifier.Engine/Dcql`, `src/Common/Sorcha.Verifier.Engine/RequestObjectValidator.cs` | partial | Final 1.0 dialect: DCQL query (`dcql_query`, Presentation Exchange retired) + multi-credential / alternative asks (Feature 181 US1/US2); verifier request objects signed with an X.509 verifier certificate carrying an `x5c` chain and a prefixed `x509_san_dns:` client_id, holder-side three-state authentication via `RequestObjectValidator` (US6). Verifier endpoint per spec 098 |
| HAIP 1.0 | 2025-12 | OpenID Foundation | [HAIP 1.0](https://openid.net/specs/openid4vc-high-assurance-interoperability-profile-1_0.html) | `src/Services/Sorcha.Haip.Service` | partial | Aligned with final HAIP wire shape: `dc+sd-jwt`, DCQL, signed request objects (Feature 181). Wire boundary classical-only per spec; PQC is internal |
| W3C Verifiable Credentials Data Model 2.0 | 2025 | W3C | [VC Data Model 2.0](https://www.w3.org/TR/vc-data-model-2.0/) | `src/Common/Sorcha.Cryptography/SdJwt`, `src/Services/Sorcha.Wallet.Service` | full | SD-JWT VC profile; `dc+sd-jwt` final media type on issue (verify dual-accepts legacy `vc+sd-jwt`); classical embedding for wire compatibility |
| IETF Token Status List 2024 (RFC 9972) | 2024 | IETF | [RFC 9972](https://datatracker.ietf.org/doc/html/rfc9972) | `src/Services/Sorcha.Wallet.Service/Services/Implementation/CitizenStatusListPublisher.cs`, `src/Services/Sorcha.Blueprint.Service/Services/StatusListManager.cs` | full | Per spec 095; W3C and IETF envelopes back the same bitstring |
| W3C Bitstring Status List | 2024 | W3C | [Bitstring Status List](https://www.w3.org/TR/vc-bitstring-status-list/) | `src/Services/Sorcha.Blueprint.Service/Services/StatusListManager.cs` | full | Internal-path issuance default |
| ISO 18013-5 (mdoc) | 2021 | ISO/IEC | [ISO 18013-5](https://www.iso.org/standard/69084.html) | `src/Common/Sorcha.Mdoc`, `src/Common/Sorcha.Proximity.Abstractions`, `src/Core/Sorcha.Blueprint.Engine/Credentials/MdocFormatHandler.cs`, `src/Services/Sorcha.Haip.Service/Services/MdocPresentationVerifier.cs` | partial | `mso_mdoc` issuance + OpenID4VP (online) verification, ES256/P-256 only (Feature 135). **Proximity (device retrieval)** — engagement, session key agreement, session encryption, device request/response, and **both** device-auth forms incl. `deviceMac`/COSE_Mac0 — implemented and validated against Annex D reference data (Feature 185). **BLE transport not yet implemented**, so no over-the-air exchange has been performed; correctness is established in-process, and this is not a claim of certified-reader interoperability. Issuance uses a flat namespace equal to the docType, which real mDLs separate — an interop gap for issuance. |
| DID (W3C) | 1.0 | W3C | [DID 1.0](https://www.w3.org/TR/did-core/) | `src/Common/Sorcha.Cryptography`, `src/Services/Sorcha.Wallet.Service` | partial | `did:sorcha:org:` and `did:sorcha:holder:` types; no DID resolution registry adoption |
| did:key | v0.7 | W3C CCG | [did:key](https://w3c-ccg.github.io/did-key-spec/) | `src/Common/Sorcha.ServiceClients.Http/Did/KeyDidResolver.cs` | partial | Offline resolution of Ed25519, P-256, and secp256k1 (`0xe701`, Feature 177) keys |
| did:jwk | Draft | Community | [did:jwk](https://github.com/quvox/did-jwk-spec) | `src/Common/Sorcha.ServiceClients.Http/Did/JwkDidResolver.cs` | full | All-curve offline resolution (Feature 177) |
| JOSE ES256K (secp256k1) | RFC 8812 | IETF | [RFC 8812](https://datatracker.ietf.org/doc/html/rfc8812) | `src/Common/Sorcha.Cryptography.Secp256k1`, `src/Common/Sorcha.Cryptography/SdJwt/SdJwtService.cs`, `src/Common/Sorcha.Verifier.Engine/VerifiablePresentationValidator.cs` | partial | Verify-only: ES256K issuer + holder VC signature verification where the DID resolves offline to a secp256k1 key (Feature 177 Phase 1), plus issuer verification against address-only DIDs by public-key recovery (Feature 178 Phase 2); signing deferred to a later phase |
| did:pkh | Draft | ChainAgnostic (CAIP-10) | [did:pkh](https://github.com/w3c-ccg/did-pkh) | `src/Common/Sorcha.ServiceClients.Http/Did/PkhDidResolver.cs` | partial | Offline `eip155` resolution to an address-only `EcdsaSecp256k1RecoveryMethod2020` VM; issuer verify-by-recovery (Feature 178) |
| did:ethr | ERC-1056 | Ethereum / DIF | [did:ethr](https://github.com/decentralized-identity/ethr-did-resolver) | `src/Common/Sorcha.ServiceClients.Http/Did/EthrDidResolver.cs`, `src/Common/Sorcha.ServiceClients.Http/Evm` | partial | Offline **default-document** resolution (Feature 178) **plus on-chain current-document resolution via read-only EVM RPC** (`eth_call`+`eth_getLogs`; owner rotation, `veriKey`/`sigAuth` delegates, `did/pub/*` key attributes with `validTo`) — **server-side only, read-only**, fail-closed on RPC error (Feature 179). Service endpoints + on-chain writes out of scope |
| EcdsaSecp256k1RecoveryMethod2020 | Draft | W3C CCG | [Recovery2020](https://identity.foundation/EcdsaSecp256k1RecoverySignature2020/) | `src/Common/Sorcha.Cryptography.Secp256k1/Secp256k1Recovery.cs`, `src/Common/Sorcha.ServiceClients.Http/Did` | partial | Verify-only: `ecrecover` an ES256K signature and match the recovered EIP-55 address to the DID's `blockchainAccountId` (Feature 178) |
| ERC-191 (`personal_sign`) | ERC-191 | Ethereum | [ERC-191](https://eips.ethereum.org/EIPS/eip-191) | `src/Common/Sorcha.Cryptography.Secp256k1/Eip191.cs`, `src/Common/Sorcha.Cryptography.Secp256k1/Secp256k1Signer.cs` | partial | Recoverable secp256k1 **signing** (RFC-6979 low-s, `r‖s‖v`) + personal-sign digest for prove-control (Feature 180); native ETH transacting shipped separately (Feature 182, see EIP-1559 row) |
| EIP-4361 (Sign-In With Ethereum) | EIP-4361 | Ethereum | [EIP-4361](https://eips.ethereum.org/EIPS/eip-4361) | `src/Common/Sorcha.Cryptography.Secp256k1/Siwe`, `src/Core/Sorcha.Wallet.Core/Services/Implementation/EthereumIdentityService.cs`, `src/Services/Sorcha.Wallet.Service/Endpoints/EthereumEndpoints.cs` | partial | SIWE format/parse + produce (wallet signs) and verify (Sorcha as relying party) prove-control, via a wallet auxiliary Ethereum identity derived from the seed at `m/44'/60'/0'/0/x` (Feature 180) |
| EIP-1559 / EIP-2718 (type-2 tx) + RLP | EIP-1559, EIP-2718 | Ethereum | [EIP-1559](https://eips.ethereum.org/EIPS/eip-1559) | `src/Common/Sorcha.Cryptography.Secp256k1/Rlp.cs`, `src/Common/Sorcha.Cryptography.Secp256k1/EthereumTransaction.cs`, `src/Common/Sorcha.ServiceClients.Http/Evm/EvmRpcClient.cs`, `src/Services/Sorcha.Wallet.Service/Services/Implementation/EthereumTransactionService.cs`, `src/Services/Sorcha.Wallet.Service/Endpoints/EthereumTransactionEndpoints.cs` | partial | Native ETH transfers: pure-managed RLP + EIP-1559 type-2 build/sign (no Nethereum), write EVM RPC broadcast (`eth_sendRawTransaction`/`getTransactionCount`/`estimateGas`/fees/receipt), gated (testnet-first, value cap, fee ceiling, `CanTransactEthereum`), **server-side only**, fire-and-report-hash (Feature 182 Phase 4). Contract writes / calldata / mainnet-by-default out of scope |
| ETSI TS 119 612 (Trusted Lists) | v2.2.1 | ETSI | [TS 119 612](https://www.etsi.org/deliver/etsi_ts/119600_119699/119612/02.02.01_60/ts_119612v020201p.pdf) | `src/Services/Sorcha.Tenant.Service/Trust/TrustedListImportService.cs`, `src/Services/Sorcha.Tenant.Service/Models/TrustedListSnapshot.cs`, `src/Services/Sorcha.Tenant.Service/Storage/TrustedListSnapshotStore.cs` | partial | Operator imports a signed trusted-list snapshot (enveloped XMLDSig core verify, TS 119 612 parse, granted CA/QC anchor extraction); Blueprint + HAIP resolve CA anchors from it for the `x509-lotl` / `trustlist` trust source (Feature 181 US3). Live LOTL / pivot-chain refresh + XAdES deferred |
| OAuth 2.0 | 2012 | IETF | [RFC 6749](https://datatracker.ietf.org/doc/html/rfc6749) | `src/Services/Sorcha.Tenant.Service` | full | JWT issuer for service-to-service and user authentication |
| SLH-DSA (FIPS 205) | 2024 | NIST | [FIPS 205](https://csrc.nist.gov/pubs/fips/205/final) | `src/Common/Sorcha.Cryptography/Core/PqcSignatureProvider.cs`, `src/Common/Sorcha.Cryptography/Core/CryptoModule.cs` | full | Internal signing path (128s/192s); HAIP wire boundary remains classical per HAIP 1.0 |
| BBS+ Signatures | Draft | IETF | [BBS Signatures](https://datatracker.ietf.org/doc/draft-irtf-cfrg-bbs-signatures/) | n/a | planned | Selective-disclosure signature scheme; SD-JWT VC currently covers the disclosure use case |

## Sorcha-implemented capabilities

Internal cross-service capabilities that aren't external standards but are referenced from agent-facing surfaces (`llms.txt`, `docs/`, OpenAPI). Each links to the spec that defines the contract.

- **Notifications & Inbox** — `full` — [specs/118-notifications-architecture/spec.md](specs/118-notifications-architecture/spec.md). Five-hub topology (Blueprint, Wallet, Register, Tenant, Chat) with Redis backplane, thin-signal contract (opaque IDs only), and durable per-user inbox with category filtering. ChatHub is the documented streaming exception (FR-019).

## Maintenance

- **PR checklist requirement.** Every PR that touches a path listed in any Components cell MUST review this file and update it if the change affects compliance status. The PR template at `.github/pull_request_template.md` carries a "STANDARDS.md reviewed" checkbox.
- **Structural CI check.** A CI step in `.github/workflows/ai-discoverability-check.yml` (via `scripts/check-discoverability.sh`) verifies on every PR to master that this file is present, parses as a Markdown table with the seven required columns, every Status is one of `full`/`partial`/`planned`, and every Components path resolves to a real path in the repository.
- **Cross-reference contract.** A second CI check verifies every standard named in `llms.txt`, `docs/llms-full.txt`, the served OpenAPI document's `info.x-standards`, and the `standards[]` frontmatter of published `docs/` files matches a row here with status `full` or `partial`. A `planned` row cannot be cited externally.
- **Drift policy.** A stale row is treated as a defect, not a documentation issue. If a Components path is renamed or a standard's adoption status changes, the same PR that makes the change updates this file.
