{
  "actor": {
    "name": "verification-analyst",
    "description": "Feature 107 PR 3 — unattended Acme Verification Co. verification analyst (n1 remote variant). Picks up pending identity applications from its inbox and auto-approves them, producing the AssuredIdentityCredential offer that the citizen claims."
  },
  "connection": {
    "gatewayUrl": "https://n1.sorcha.dev",
    "registerId": "{{registerId}}",
    "credentials": {
      "email": "{{roles.verificationAnalyst.email}}",
      "password": "$env:VERIFICATION_ANALYST_PASSWORD",
      "organizationId": "{{roles.verificationAnalyst.organizationId}}"
    },
    "walletAddress": "{{roles.verificationAnalyst.walletAddress}}"
  },
  "inbox": {
    "signalR": { "enabled": true },
    "polling": { "enabled": true, "intervalSeconds": 10 }
  },
  "mode": "rules",
  "rules": [
    {
      "actionName": "Verify Assured Identity Application",
      "decision": "approve",
      "payload": {
        "decision": "approved",
        "verificationNotes": "Auto-approved by the verification-analyst demo agent (n1 remote). Fields checked: persona-filled name, DOB, email, structured address (and portrait when present)."
      }
    }
  ],
  "logging": {
    "level": "Information",
    "actionLog": "./logs/verification-analyst-actions.jsonl"
  }
}
