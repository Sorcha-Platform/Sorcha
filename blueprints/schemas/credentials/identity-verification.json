{
  "identifier": "identity-verification",
  "title": "Identity Verification",
  "description": "Identity verification record capturing the method used and timestamp of successful verification",
  "version": "1.0.0",
  "category": "credentials",
  "tags": ["identity", "verification", "kyc", "biometric", "credential"],
  "keywords": ["identity", "verification", "biometric", "document", "knowledge", "verified"],
  "schema": {
    "type": "object",
    "properties": {
      "fullName": {
        "type": "string",
        "title": "Full Name",
        "description": "Full name of the verified individual",
        "minLength": 1,
        "maxLength": 100
      },
      "dateOfBirth": {
        "type": "string",
        "title": "Date of Birth",
        "description": "Date of birth of the verified individual",
        "format": "date"
      },
      "verificationMethod": {
        "type": "string",
        "title": "Verification Method",
        "description": "Method used to verify identity",
        "enum": ["document", "biometric", "knowledge"]
      },
      "verifiedAt": {
        "type": "string",
        "title": "Verified At",
        "description": "Date and time the verification was completed",
        "format": "date-time"
      }
    },
    "required": ["fullName", "dateOfBirth", "verificationMethod", "verifiedAt"]
  },
  "formLayout": {
    "type": "VerticalLayout",
    "elements": [
      { "type": "TextLine", "scope": "fullName", "title": "Full Name" },
      { "type": "TextLine", "scope": "dateOfBirth", "title": "Date of Birth" },
      {
        "type": "HorizontalLayout",
        "elements": [
          { "type": "Dropdown", "scope": "verificationMethod", "title": "Verification Method" },
          { "type": "TextLine", "scope": "verifiedAt", "title": "Verified At" }
        ]
      }
    ]
  },
  "disclosure": {
    "sensitive": ["dateOfBirth"],
    "recommendation": "Identity verification results confirm a person's identity without exposing underlying documents. Share verification status broadly but restrict date of birth to participants with a legitimate need."
  }
}
