{
  "identifier": "right-to-work",
  "title": "Right to Work",
  "description": "Right to work verification with document type, nationality, and expiry for employment eligibility checks",
  "version": "1.0.0",
  "category": "credentials",
  "tags": ["right-to-work", "employment", "visa", "immigration", "credential"],
  "keywords": ["right to work", "passport", "visa", "permit", "nationality", "employment"],
  "schema": {
    "type": "object",
    "properties": {
      "documentType": {
        "type": "string",
        "title": "Document Type",
        "description": "Type of right to work evidence",
        "enum": ["passport", "visa", "permit", "settled_status"]
      },
      "nationality": {
        "type": "string",
        "title": "Nationality",
        "description": "Nationality of the individual",
        "minLength": 1,
        "maxLength": 100
      },
      "expiryDate": {
        "type": "string",
        "title": "Expiry Date",
        "description": "Date the document or right expires",
        "format": "date"
      }
    },
    "required": ["documentType", "nationality"]
  },
  "formLayout": {
    "type": "VerticalLayout",
    "elements": [
      { "type": "Dropdown", "scope": "documentType", "title": "Document Type" },
      { "type": "TextLine", "scope": "nationality", "title": "Nationality" },
      { "type": "TextLine", "scope": "expiryDate", "title": "Expiry Date" }
    ]
  },
  "disclosure": {
    "sensitive": ["documentType", "nationality"],
    "recommendation": "Right to work data is sensitive immigration information. Only disclose to HR or compliance participants conducting employment eligibility checks. Never share with general workflow participants."
  }
}
