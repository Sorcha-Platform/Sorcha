{
  "name": "sorcha-reader",
  "version": "1.0.0",
  "private": true,
  "description": "The Sorcha Reader — the verifier's half of an in-person credential check. Wraps the Blazor WASM reader (Sorcha.Verifier.Pwa) and the sorcha-proximity BLE plugin in its CENTRAL role.",
  "scripts": {
    "sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.4.0",
    "sorcha-proximity": "file:../plugins/sorcha-proximity"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
