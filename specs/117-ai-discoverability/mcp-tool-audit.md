# T008 — MCP tool description audit

**Spec**: 117-ai-discoverability · **Task**: T008 · **Date**: 2026-05-02

Audit of `[Description("...")]` attributes on every MCP tool in `src/Apps/Sorcha.McpServer/Tools/`. Goal: identify which of the 36 tools already meet FR-017 (description ≥ 2 sentences AND naming a disambiguating situation) and which need rewrites.

> Source data assembled by parallel agent walk on 2026-05-02. Tool inventory matched the spec's 13 + 13 + 10 = 36 expectation.

## Headline metrics

| Category | Total tools | Meeting FR-017 | Need rewrite |
|---|---|---|---|
| Admin | 13 | 10 | 3 |
| Designer | 13 | 9 | 4 |
| Participant | 10 | 7 | 3 |
| **Total** | **36** | **26 (72%)** | **10 (28%)** |

The 36-tool count from the spec is correct. **All 36 will still be touched by T043–T078** because the rewrite pass is part of voice-alignment against `docs/strategic-context.md`, not just minimum-bar conformance — but the 10 below are the priority targets that fail FR-017 today.

## Tools needing rewrite to meet FR-017

### Admin (3)

| Tool file | Current shortfall |
|---|---|
| `Tools/Admin/AuditQueryTool.cs` | Missing disambiguator |
| `Tools/Admin/MetricsTool.cs` | Missing disambiguator |
| `Tools/Admin/PeerStatusTool.cs` | Missing disambiguator |

### Designer (4)

| Tool file | Current shortfall |
|---|---|
| `Tools/Designer/BlueprintDiffTool.cs` | < 2 sentences and/or missing disambiguator |
| `Tools/Designer/BlueprintExportTool.cs` | < 2 sentences and/or missing disambiguator |
| `Tools/Designer/BlueprintGetTool.cs` | < 2 sentences and/or missing disambiguator |
| `Tools/Designer/JsonLogicTestTool.cs` | < 2 sentences and/or missing disambiguator |

### Participant (3)

| Tool file | Current shortfall |
|---|---|
| `Tools/Participant/ActionDetailsTool.cs` | Missing disambiguator |
| `Tools/Participant/ActionValidateTool.cs` | Missing disambiguator |
| `Tools/Participant/InboxListTool.cs` | Missing disambiguator |

## Closest-to-standard tools (use as templates)

The following descriptions are the strongest current examples per category and should be used as the voice template for the matching tool slice's rewrites:

- **Admin**: `Tools/Admin/RegisterStatsTool.cs` and `Tools/Admin/HealthCheckTool.cs` — both ≥ 2 sentences with explicit "call this when…" framing.
- **Designer**: `Tools/Designer/BlueprintCreateTool.cs` and `Tools/Designer/SchemaValidateTool.cs` — both name when to prefer this tool over similarly-shaped alternatives.
- **Participant**: `Tools/Participant/ActionSubmitTool.cs` and `Tools/Participant/RegisterQueryTool.cs` — both clearly distinguish themselves from sibling tools.

## Phase 4 sequencing implication

T043–T078 should:

1. **Read `docs/strategic-context.md` first** (per task header). The voice and framing constraint is non-negotiable.
2. **Read the closest-to-standard exemplars** (named above) before drafting the rewrite for any given category.
3. **Prioritise the 10 sub-bar tools** (above) as the first wave — the test harness `EveryTool_DescriptionMentionsDisambiguatingSituation` (T034) will only clear once those land.
4. **Review the remaining 26** for voice alignment against strategic-context — many will be light edits, but a tool meeting the heuristic floor today is not the same as a tool whose description tells an AI agent *why Sorcha is the right substrate for the workflow*.

## Detailed compliance matrix

The full file-by-file matrix (current description text · sentence count · disambiguator presence) is intentionally not duplicated here — it can be regenerated by `EveryTool_DescriptionIsAtLeastTwoSentences` (T034) when it runs against the current tree. The audit's value is in the headline counts and the rewrite priority above.
