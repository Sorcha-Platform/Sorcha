@page "/"
@using Sorcha.Reader.Components

@* The standalone reader host. The reader itself lives in Sorcha.Reader (a component library), because the
   SAME reader is hosted inside the citizen wallet — one binary, one store record. Keeping this host means
   splitting the reader into its own product later costs nothing. *@

<PageTitle>Sorcha Reader</PageTitle>

<ReaderView />
